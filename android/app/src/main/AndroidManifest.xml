<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.audiocapture.recorder">

    <!-- ======================== PERMISSIONS ======================== -->

    <!-- Izin rekam audio dari Mikrofon -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Izin tulis file ke storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ storage permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Wajib untuk Foreground Service agar tidak dimatikan OS -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Wajib untuk MediaProjection (screen/audio capture) -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <!-- Mencegah sleep saat merekam -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Notification (wajib untuk Foreground Service di Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="Audio Recorder"
        android:name="${applicationName}"
        android:icon="@android:drawable/ic_btn_speak_now"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ======================== FOREGROUND SERVICE ======================== -->
        <!--
            Service ini WAJIB dideklarasikan dengan type="mediaProjection"
            agar Android mengizinkan capture audio dari aplikasi lain.
            Tanpa ini, AudioPlaybackCaptureConfiguration akan gagal di Android 10+
        -->
        <service
            android:name=".AudioCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
